"use strict";

// POST /api/password-reset

const express = require("express");
const router = express.Router();
const mongoose = require("mongoose");
const Usuario = mongoose.model("Usuario");

app.route("/password-reset").post(userCtrl.password - reset);
